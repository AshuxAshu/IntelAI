# Training diary

One entry per training run that needed iteration. The loop: a behavioral failure
is observed (symptom), attributed to a cause (diagnosis), answered with targeted
data or configuration (data fix), and re-measured (result). Entries append in
chronological order; run ids reference the runs/ provenance records.

## <run_id>

- symptom:
- diagnosis:
- data fix:
- result:
